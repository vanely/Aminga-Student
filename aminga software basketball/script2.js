const motorista = {
  name: "Alexandre" ,
  age:15 ,
  height:195 ,
  weight:80 , }
{
        
    }

const drivingAge = 18

if (motorista.age >= drivingAge) {
  console.log(`${motorista.name}can drive`)
}else{
    console.log(`${motorista.name}can't drive`)
}