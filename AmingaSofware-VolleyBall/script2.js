const motorista = {
    name: "Dainelly Delgado",
    age: 17,
    height: 162,
    weight: 55
}

const drivingAge = 18

if (motorista.age >= drivingAge) {
    console.log(`${motorista.name} can drive a car`)
}else{
    console.log(`${motorista.name} can not drive a car`)
}